{
  "name": "@istqb-app/shared",
  "version": "1.0.9",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "lint": "echo 'No lint configured for shared package'",
    "test": "echo 'No tests configured for shared package (types only)'"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
